using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CoinItem : Item
{
    // Start is called before the first frame update
    void Start()
    {
        InitState(ItemType.COIN);
    }

    // Update is called once per frame
    void Update()
    {
        
    }


}
