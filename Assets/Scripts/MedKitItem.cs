using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MedKitItem : Item
{
    // Start is called before the first frame update
    void Start()
    {
        InitState(ItemType.MED_KIT);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
